class main {
	visible bool esPrimo;
	hidden numeral a, b, c;
	visible real x, y, z;

	visible without main () {
		numeral n, m;
		real s, t;
		numeral c, a;

		this.a = 2;
		a = 5;
		x = 3.5;
		this.b = a * this.a - (2 + 21) % a;

		y = (this.b - (45 / 4) * (99 + this.a - x)) / (34 + this.b);

		output(this.a);
		output(x);
		output(a);
		output(this.b);
		output(y);
	} 
}